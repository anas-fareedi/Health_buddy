# Core Framework
# Flask 3.1.x and Werkzeug 3.1.x must stay in sync —
# Werkzeug 3.1 removed internal APIs that Flask 3.0 relied on.
Flask>=3.1.0
Flask-Talisman>=1.1.0
Werkzeug>=3.1.0
python-dotenv>=1.0.0

# LangChain and AI
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-pinecone>=0.1.0
langchain-huggingface>=0.0.1
langchain-google-genai>=1.0.0

# Vector DB (client only — no local model loading)
pinecone-client>=3.0.0

# Google AI
google-generativeai>=0.3.0

# Utilities
pypdf>=4.0.1
huggingface-hub>=0.20.0

# NOTE: torch and sentence-transformers are intentionally excluded for cloud deployment.
# On Render's 512MB RAM tier, PyTorch alone exceeds memory limit.
# Embeddings are served via HuggingFace Inference API (requires HF_TOKEN).

# WSGI Server
gunicorn>=21.2.0

# Security
flask-limiter>=3.5.0